// firebase-firestore.js — PLACEHOLDER (no es el SDK real). Ver README.md y firebase-app.js.
// SDK objetivo: Firebase Web modular v10.12.2 (firebase-firestore.js). Sin claves ni tokens.

export const __PLACEHOLDER__ = true;

function notVendored() {
  throw new Error('[firebase-vendor] SDK no vendorizado. Coloca el firebase-firestore.js real en libs/firebase/ (ver README.md).');
}

export function getFirestore() { notVendored(); }
export function doc() { notVendored(); }
export function getDoc() { notVendored(); }
export function setDoc() { notVendored(); }
export function updateDoc() { notVendored(); }
export function serverTimestamp() { notVendored(); }
